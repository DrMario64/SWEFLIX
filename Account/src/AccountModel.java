public class AccountModel {
    private String account;

    /*
     * gets an account from the database given the account ID.
     * still need to make account object
     */
    /*public Account getAccount(String accountID) {
        account = searchDatabase(accountID);
    }*/

    /*
     * retrieves all accounts from the database and returns an array of them
     * still need to make account object
     */
    /*public Account[] getAccountList() {
        return getAllAccounts();
    }*/

    /*
     * retrieves all accounts that share the same username
     */
    /*public Account[] getAccounts(String username) {
        return getAccountsByUsername(username);
    }*/
}
